#include <stdio.h>
/**
 * main - main block
 *
 * Description: prints all single digit numbers starting 
 * from base 10 to 0 in a new line
 * Return: 0
 */
int main(void)
{
	int  i;

	for (i = '0' ; i = '9' ; i++)
	{
		printf("%i", i);
	}
	printf('\n');
	return (0);
}
